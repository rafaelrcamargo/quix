refresh_rate: 1 seconds

# ? Uncomment to enable logging to a file

appenders:
  console:
    kind: console
    encoder:
      pattern: "{d(%d/%m/%Y - %H:%M)} | {h({l})} | {m}{n}"
    filters:
      - kind: threshold
        level: debug
#  file:
#    kind: file
#    path: "logs/quix.log"
#    encoder:
#      pattern: "{d(%Y-%m-%d %H:%M:%S)} | {({l}):5.5} | {f}:{L} — {m}{n}"
#    filters:
#      - kind: threshold
#        level: error

root:
  level: debug
  appenders:
    - console
#loggers:
#  quix:
#    level: trace
#    appenders:
#      - file
#    additive: true
